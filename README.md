# Dragon's Lounge - events feed

`events.json` is a snapshot of the lounge public Google Calendar, refreshed
automatically every 15 minutes by GitHub Actions. The website reads it from the
raw URL, so whatever the lounge adds to its calendar shows up on the site by
itself. Nothing here needs to be run by hand.
